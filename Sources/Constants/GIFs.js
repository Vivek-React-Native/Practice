const GifPath = '../Assets/GIFs/';
const gif = '.gif';

const GIFs = {
  Butterfly: require(GifPath + 'Butterfly' + gif),
  ColorLines: require(GifPath + 'ColorLines' + gif),
  GirlHairs: require(GifPath + 'GirlHairs' + gif),
  Houses: require(GifPath + 'Houses' + gif),
  Sun: require(GifPath + 'Sun' + gif),
};

export default GIFs;
